## You will find these react components are in this repo:

- Modal

Usage Example:

```
        <Modal
          Show={true}
          OkAction={() => { //function to execute on clicking ok })}
          CancelAction={() => { //function to execute on clicking ok })}
        >
          Modal Text Content
        </Modal>
```
